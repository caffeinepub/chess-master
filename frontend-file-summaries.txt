{
  "frontend/src/types/chess.ts": "Core TypeScript type definitions for the chess game; adds the `CastlingRights` interface with four boolean flags (whiteKingside, whiteQueenside, blackKingside, blackQueenside) and includes it as a required field in `GameState`.",
  "frontend/src/utils/chess-setup.ts": "Utility to create the standard 8x8 chess starting board with all 32 Unicode pieces, plus helper functions to identify piece colors.",
  "frontend/src/utils/move-validation.ts": "Complete move validation for all chess pieces plus castling helpers; `applyCastlingMove` now explicitly types its return as `Board` (i.e. `Square[][]`) to avoid the `string[][]` mismatch, keeping all null assignments valid.",
  "frontend/src/utils/valid-moves.ts": "Move computation utilities; updated `getValidMovesForPiece`, `getValidMoves`, `getAllValidMovesForPlayer`, and `hasAnyValidMoves` to accept an optional `castlingRights` parameter, and adds castling destination squares (g1/c1 or g8/c8) to the king's valid moves when castling conditions are met.",
  "frontend/src/utils/game-status.ts": "Game status detection (checkmate/stalemate/check/draw); fixed to import `hasAnyValidMoves` from `valid-moves` (not the removed inline version) and `DrawReason` from types, with optional `drawReason` parameter support.",
  "frontend/src/components/ChessSquare.tsx": "Individual chess square with clamp()-based responsive piece font sizes, coordinate labels using viewport units, and all highlight overlays (selection, check, last-move, valid-move) that scale with square size.",
  "frontend/src/components/ChessBoard.tsx": "Chess board renderer that passes all square props to ChessSquare except `isLight` (which is now computed internally by ChessSquare from row/col), with responsive viewport-aware sizing and AI thinking/game-over overlays.",
  "frontend/src/components/GameStatus.tsx": "Game status bar restored to use the correct `GameResult`/`DrawReason` types matching the existing `GameState` interface, with responsive clocks, status message, and optional mute toggle.",
  "frontend/src/pages/ChessGame.tsx": "Main chess game page using the correct existing `GameState` type (with `currentPlayer`, `selectedPiece`, `gameOver`, `isCheck`), proper `getGameStatus` API (checking `status.status`), correct `trackPosition`/`hasThreefoldRepetition` signatures with `PositionHistory` ref, and `getValidMoves` with `Player` as third argument.",
  "frontend/src/App.tsx": "Root application component that renders the MobileAppNotice banner (mobile-only) above the main ChessGame page.",
  "frontend/src/index.css": "Global styles with OKLCH chess theme, mobile-first base rules including overflow-x hidden, iOS input zoom prevention (font-size 16px), minimum 44px touch targets for buttons, and chess board/piece/overlay CSS classes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with xs breakpoint (375px) added to screens, chess semantic color tokens, Playfair Display/EB Garamond fonts, and chess-specific shadow utilities.",
  "frontend/src/utils/ai-player.ts": "Minimax AI with alpha-beta pruning; updated to accept and propagate `castlingRights` through all move generation and evaluation calls, uses `applyCastlingMove` for castling moves, and updates rights after each simulated move so the AI can correctly evaluate and execute castling.",
  "frontend/src/components/GameModeSelector.tsx": "Four-button game mode selector using the correct `GameMode` type from `types/chess.ts` (`two-players`, `one-player`, `auto-play`, `online`) with mobile-friendly grid layout.",
  "frontend/src/hooks/useMoveSound.ts": "New custom hook that synthesizes a short percussive chess piece placement sound using the Web Audio API (oscillator + noise burst), exposing a `playMoveSound()` function with no external network requests.",
  "frontend/src/hooks/useBackgroundMusic.ts": "New custom hook that generates and manages a looping ambient C-major pentatonic melody using the Web Audio API scheduler pattern, exposing `startMusic()`, `stopMusic()`, `toggleMute()`, `isMuted`, and `isPlaying` state.",
  "frontend/src/utils/position-tracker.ts": "New utility module that serializes board positions (board + active player) into string keys and tracks occurrence counts in a Map to detect threefold repetition; exports serializePosition, trackPosition, hasThreefoldRepetition, and clearPositionHistory.",
  "frontend/src/hooks/useGetCallerUserProfile.ts": "React Query hook that fetches the authenticated caller's user profile from the backend, with proper loading state that accounts for actor initialization delay to prevent profile setup modal flash.",
  "frontend/src/hooks/usePlayerStats.ts": "React Query hook that fetches the authenticated player's stats from the backend with retry/refetch logic and proper auth gating; also exports `useRecordAIMatchResult` mutation hook that calls `actor.recordAIMatchResult` and invalidates `playerStats` and `leaderboard` queries on success.",
  "frontend/src/hooks/useLeaderboard.ts": "React Query hook that fetches the global leaderboard (all players sorted by points) from the backend, polling every 60 seconds.",
  "frontend/src/hooks/useOnlineGame.ts": "React Query hook managing online game state with 1.5-second polling; includes mutations for createGame and joinGame backend calls.",
  "frontend/src/components/LoginButton.tsx": "Login/logout button with minimum 44px touch target; replaces all `flex-shrink-0` with `shrink-0` per Tailwind v3.",
  "frontend/src/components/PlayerIdentity.tsx": "Player name display with truncation; replaces `flex-shrink-0` with `shrink-0` per Tailwind v3.",
  "frontend/src/components/ProfileSetupModal.tsx": "Profile setup modal with responsive width (calc(100vw-32px)), 16px input font size to prevent iOS auto-zoom, and 44px minimum touch targets for all interactive elements.",
  "frontend/src/components/PlayerStats.tsx": "Compact player stats badges; replaces all `flex-shrink-0` with `shrink-0` per Tailwind v3.",
  "frontend/src/components/GameResultsPanel.tsx": "Game result overlay modal showing win/loss/draw outcome with icon, title, optional points earned, and New Game/Close buttons; fixes icon variable type to `React.ReactNode` to resolve TS2322 errors.",
  "frontend/src/components/Leaderboard.tsx": "Leaderboard with fixed-column grid layout and ScrollArea; replaces all `flex-shrink-0` with `shrink-0` per Tailwind v3.",
  "frontend/src/components/OnlineGameSetup.tsx": "Online game setup component using the correct `useOnlineGame` hook API with `createGameMutation`/`joinGameMutation`, and `onGameReady` callback prop matching the existing interface.",
  "frontend/src/utils/backend-sync.ts": "Utility functions to convert between frontend Unicode chess piece representation and backend Piece object format; exports frontendToBackendBoard and backendToFrontendBoard.",
  "frontend/src/pages/OnlineChessGame.tsx": "Online multiplayer chess page using the correct `useOnlineGame` hook API (`gameQuery.data`/`gameQuery.isLoading`), proper `GameState` field names, correct `getGameStatus`/`trackPosition`/`hasThreefoldRepetition` signatures, and `getValidMoves` with `Player` as third argument.",
  "frontend/index.html": "HTML entry point with proper mobile viewport meta tag allowing user zoom up to 5x while preventing unwanted auto-zoom.",
  "frontend/src/components/MobileAppNotice.tsx": "Dismissible fixed banner shown only on mobile viewports that informs users no Android APK is available and suggests using the browser's \"Add to Home Screen\" feature; dismissal state is persisted in localStorage.",
  "frontend/src/utils/urlParams.ts": "URL and session parameter utilities including `getSecretParameter` (required by `useActor.ts`), hash-based secret extraction, session storage helpers, and game ID persistence helpers."
}